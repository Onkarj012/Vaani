// NavItem is now handled directly by Sidebar.tsx (design-6 style).
// This stub is kept to avoid breaking any external imports.
export function NavItem(_props: { to: string; icon: unknown; label: string }) {
  return null;
}
