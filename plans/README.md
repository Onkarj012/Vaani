# Implementation Plans

Generated by the improve skill on 2026-06-16. Execute in the order below unless dependencies say otherwise. Each executor should read the assigned plan fully, honor its STOP conditions, and update the status row when done.

## Execution Order & Status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Move provider API keys out of settings and exports | P1 | M | — | DONE |
| 002 | Validate the submitted provider API key | P1 | M | — | DONE |
| 003 | Add CSP to main and overlay renderer entries | P1 | S | — | DONE |
| 004 | Serialize history store writes | P1 | S | — | DONE |
| 005 | Guard overlay prompt listeners against window creation races | P1 | S | — | DONE |
| 006 | Run verification before release packaging | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale).

## Dependency Notes

- Plan 001 should land before any broader provider-settings or credential UI work. It changes the security boundary for provider keys.
- Plan 004 should land before a future history pagination/indexing plan, because serialized writes are the safer baseline.
- Plan 005 should land before a future overlay lifecycle consolidation, because it fixes the immediate response-listener race without broad window-management churn.
- Plan 006 is a good early guardrail before package upgrades, release changes, or higher-risk refactors.

## Findings Planned

- Security: API keys are persisted through settings and included in exports.
- Correctness: API-key testing ignores the submitted key.
- Security: main and overlay renderers lack CSP while recorder has one.
- Correctness: history JSON mutations are not serialized.
- Correctness: overlay prompt response listeners can be skipped during async window creation.
- Tests/DX: release packaging lacks test/typecheck gates.

## Findings Considered And Not Planned In This Batch

- Correct privacy docs for opt-in local recording saves: valid docs fix, but lower risk than the first six plans.
- Add pagination/summary APIs for history: valid performance plan; do after Plan 004.
- Add macOS native smoke/integration layer: valid test coverage improvement; do after Plan 006.
- Include tests in TypeScript checking: useful DX improvement; lower priority than release verification.
- Package-manager standardization: real reproducibility concern, but not necessary before the current top fixes.
- Dependency upgrades: security-relevant but needs advisory reachability triage and package-manager decision.
- Split `DictationService`: valid architecture work, high risk; requires characterization tests first.
- Make offline mode a real routing policy: valid direction item; should start as a design/spike plan.
- App profiles product surface: valid direction item; should start as a design/spike plan.
